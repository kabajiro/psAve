% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.psave}
\alias{predict.psave}
\title{Predict averaged scores for new data}
\usage{
\method{predict}{psave}(object, newdata, type = c("ps", "prog"), ...)
}
\arguments{
\item{object}{A \code{psave} object fitted with \code{keep.fits = TRUE}.}

\item{newdata}{A data frame containing the variables of the propensity
score formula (for \code{type = "ps"}) or of the prognostic specification (for
\code{type = "prog"}). Missing values are an error.}

\item{type}{\code{"ps"} (default) or \code{"prog"}.}

\item{...}{Ignored.}
}
\value{
A numeric vector with one score per row of \code{newdata}, named by its
rownames. If \code{newdata} is missing, the in-sample fitted scores are
returned (equivalent to \code{\link[=fitted.psave]{fitted.psave()}}).
}
\description{
Computes the model-averaged propensity score (or prognostic score) for new
observations by applying the stored candidate fits to \code{newdata}, clipping
candidate propensity scores as at fit time, and combining them with the
selected mixing weights. Requires \code{psave(..., keep.fits = TRUE)}.
}
\seealso{
\code{\link[=psave]{psave()}}, \code{\link[=fitted.psave]{fitted.psave()}}
}
