% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{simplex_grid}
\alias{simplex_grid}
\title{Enumerate the discretized probability simplex}
\usage{
simplex_grid(M, step = 0.05)
}
\arguments{
\item{M}{Integer; the number of mixture components (grid columns).}

\item{step}{Numeric; the grid increment. Must evenly divide 1 (checked in
integer arithmetic: with \code{n_steps = round(1/step)}, the call errors unless
\code{abs(n_steps * step - 1) < 1e-8}). Default \code{0.05}, the value used in
Kabata, Stuart and Shintani (2024).}
}
\value{
A numeric matrix with \eqn{\binom{S + M - 1}{M - 1}} rows and \code{M}
columns; each row sums to 1 exactly (in integer arithmetic before the
single final division by \eqn{S}).
}
\description{
Enumerates all points of the probability simplex
\eqn{\{\lambda \in [0,1]^M : \sum_m \lambda_m = 1\}} on a regular grid with
increment \code{step}, using \strong{integer arithmetic}: with \eqn{S = 1/\code{step}}
steps, every integer composition \eqn{(c_1, \dots, c_M)} with
\eqn{\sum_m c_m = S} and \eqn{c_m \ge 0} is listed and returned as
\eqn{c / S}. This is the grid over which \code{\link[=psave]{psave()}} searches for the
propensity score mixing weights \eqn{\lambda} and the prognostic mixing
weights \eqn{\gamma}.
}
\details{
The number of grid points is exactly \eqn{\binom{S + M - 1}{M - 1}}; e.g.,
\code{M = 4}, \code{step = 0.05} gives \code{choose(23, 3) = 1771} points. Because the grid
is built from integer compositions, every valid point is present by
construction; the reference implementation of the paper instead filtered
\code{expand.grid()} rows with a floating-point \code{rowSums(gr) == 1} test, which
silently dropped about 10.6\% of the valid points for \code{M = 4},
\code{step = 0.05}.

\strong{Enumeration order (the tie-breaking rule).} Rows are generated by
recursive descent: \eqn{c_1} runs from \eqn{S} down to 0; within each value
of \eqn{c_1}, \eqn{c_2} runs from the remainder down to 0; and so on. The
first row is therefore \eqn{(1, 0, \dots, 0)} and the last row is
\eqn{(0, \dots, 0, 1)}. All grid searches in \code{\link[=psave]{psave()}} resolve ties by
taking the \strong{first} row attaining the minimum, within a 1e-9 relative
tolerance of the minimum, so ties favor learners listed earlier in
\code{ps.methods} / \code{prog.methods}. The tolerance is deliberate: criterion
values are computed with floating-point matrix algebra whose lowest-order
bits can differ across BLAS implementations, so an exact bitwise
\code{which.min()} would not be reproducible across machines, whereas the
tolerant first-minimum rule is.
}
\examples{
simplex_grid(2, step = 0.25)
nrow(simplex_grid(4, step = 0.05))  # choose(23, 3) = 1771

# first row = all weight on the first component; last = on the last:
head(simplex_grid(3, step = 0.25), 3)
tail(simplex_grid(3, step = 0.25), 3)
}
\references{
Kabata D, Stuart EA, Shintani A (2024). Prognostic score-based model
averaging approach for propensity score estimation. \emph{BMC Medical Research
Methodology}, 24, 228. \doi{10.1186/s12874-024-02350-y}
}
\seealso{
\code{\link[=psave]{psave()}}, \code{\link[=psave_criteria]{psave_criteria()}}
}
