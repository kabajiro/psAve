% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.psave}
\alias{plot.psave}
\title{Plot a psave object}
\usage{
\method{plot}{psave}(x, type = c("balance", "distribution", "criterion"), ...)
}
\arguments{
\item{x}{A \code{psave} object.}

\item{type}{One of \code{"balance"} (default), \code{"distribution"}, \code{"criterion"}.}

\item{...}{For \code{"balance"}, further arguments to \code{\link[cobalt:love.plot]{cobalt::love.plot()}}
(e.g., \code{thresholds = 0.1}); otherwise further graphical parameters passed
to the base plotting calls.}
}
\value{
For \code{"balance"}, the \code{ggplot} object from \code{\link[cobalt:love.plot]{cobalt::love.plot()}}
(invisibly, after printing); otherwise \code{x}, invisibly.
}
\description{
Three diagnostic displays for a \code{\link[=psave]{psave()}} fit:
\describe{
\item{\code{"balance"}}{a Love plot of covariate and prognostic-score balance
before/after weighting, via \code{\link[cobalt:love.plot]{cobalt::love.plot()}} (dispatched through
\code{\link[=bal.tab.psave]{bal.tab.psave()}}; \pkg{cobalt} is an Import, so always available).}
\item{\code{"distribution"}}{the distribution of the propensity scores by
treatment group: grey curves for the candidate models, a colored curve
for the selected average (base graphics). Inspect this for extreme
candidate scores.}
\item{\code{"criterion"}}{the selection criterion over the \eqn{\lambda} grid:
exact for up to three candidates (a curve for M = 2, a colored grid map
for M = 3); for M > 3, one profile per candidate (the minimum criterion
value attainable at each value of \eqn{\lambda_m}). Requires the stored
\code{path} (\code{keep.path = TRUE} and a grid of at most 100,000 rows).}
}
}
\seealso{
\code{\link[=psave]{psave()}}, \code{\link[=bal.tab.psave]{bal.tab.psave()}}, \code{\link[cobalt:love.plot]{cobalt::love.plot()}}
}
