% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{psave_match}
\alias{psave_match}
\title{Match on a model-averaged propensity score}
\usage{
psave_match(x, ...)
}
\arguments{
\item{x}{A \code{psave} object.}

\item{...}{Arguments forwarded verbatim to \code{\link[MatchIt:matchit]{MatchIt::matchit()}} (e.g.,
\code{method}, \code{caliper}, \code{ratio}, \code{replace}).}
}
\value{
A \code{matchit} object; see \code{\link[MatchIt:matchit]{MatchIt::matchit()}}.
}
\description{
Convenience pass-through to \code{\link[MatchIt:matchit]{MatchIt::matchit()}}: matches on the
model-averaged propensity score of a \code{\link[=psave]{psave()}} fit, reusing the formula and
data stored in the object. Equivalent to the canonical explicit call
\preformatted{MatchIt::matchit(<formula>, data = <data>, distance = fit$ps, ...)}
but with no opportunity for row misalignment between the two steps. All
\code{...} arguments are forwarded verbatim; the return value is an ordinary
\code{matchit} object, so the full \pkg{MatchIt}/\pkg{cobalt} toolkit applies.
}
\examples{
\dontshow{if (requireNamespace("MatchIt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("lalonde", package = "MatchIt")
fit <- psave(treat ~ age + educ + married + re74, data = lalonde,
             outcome = ~ re78, ps.methods = "glm", prog.methods = "glm")
m <- psave_match(fit, method = "nearest", caliper = 0.2)
cobalt::bal.tab(m, distance = data.frame(prog = fit$prog))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=psave]{psave()}}, \code{\link[=psave_weight]{psave_weight()}}, \code{\link[MatchIt:matchit]{MatchIt::matchit()}}
}
