% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{psave_criteria}
\alias{psave_criteria}
\title{Compute all four psAve selection criteria for a propensity score vector}
\usage{
psave_criteria(
  ps,
  treat,
  covs,
  prog = NULL,
  estimand = c("ATT", "ATE"),
  s.d.denom = "treated",
  bin.vars = NULL
)
}
\arguments{
\item{ps}{Numeric vector of propensity scores, strictly inside (0, 1).}

\item{treat}{Treatment vector; coerced to 0/1 like the left-hand side of
the \code{\link[=psave]{psave()}} formula (numeric 0/1, logical, or two-level
factor/character with the second level treated).}

\item{covs}{Numeric matrix (or all-numeric data frame) of covariates, one
row per unit. Factors must already be expanded to dummy columns (as in
the \code{covs} component of a \code{psave} object).}

\item{prog}{Optional numeric vector: a prognostic score. If \code{NULL}, the
\code{prog} criterion is returned as \code{NA}.}

\item{estimand}{\code{"ATT"} (default) or \code{"ATE"}; determines the weights (see
Details).}

\item{s.d.denom}{Group whose (unweighted) standard deviation standardizes
the mean differences: \code{"treated"} (default; the paper's convention for
BOTH estimands), \code{"control"}, \code{"pooled"}, or \code{"all"}.}

\item{bin.vars}{Optional logical vector flagging binary columns of \code{covs},
used for the KS criterion only; if \code{NULL}, columns with exactly two
distinct values are detected automatically. The \code{smd} and \code{prog} criteria
always use uniform sample-SD standardization (\code{bin.vars = FALSE} for all
columns), which is the convention of the published method (see Details).}
}
\value{
A named numeric vector with elements \code{logloss}, \code{smd}, \code{ks}, and
\code{prog} (the last is \code{NA} when \code{prog = NULL}).
}
\description{
Evaluates, for an arbitrary propensity score vector, the four selection
criteria used by \code{\link[=psave]{psave()}}: the treatment-assignment log loss, the mean
weighted absolute standardized mean difference (ASMD) of the covariates,
the mean weighted Kolmogorov-Smirnov (KS) statistic of the covariates, and
the weighted ASMD of a prognostic score. This function powers the
\code{diagnostics} table of a \code{psave} object (the "was averaging worth it?"
comparison) and is exported as a methods-research utility.
}
\details{
Weights are the inverse-probability weights implied by \code{ps} at \code{estimand}:
for the ATT, \eqn{W_i = 1} for treated units and \eqn{e_i/(1-e_i)} for
untreated units; for the ATE, \eqn{1/e_i} and \eqn{1/(1-e_i)}. The four
criteria are:
\describe{
\item{\code{logloss}}{\eqn{-\mathrm{mean}\{A_i \log e_i + (1-A_i)\log(1-e_i)\}}.}
\item{\code{smd}}{the mean over covariates \eqn{j} of
\eqn{|\bar X_{1j}^w - \bar X_{0j}^w| / s_j}, where \eqn{\bar X_{aj}^w}
is the weighted mean of \eqn{X_j} in arm \eqn{a} and \eqn{s_j} is the
unweighted sample SD of \eqn{X_j} in the \code{s.d.denom} group. Computed via
\code{\link[cobalt:col_w_smd]{cobalt::col_w_smd()}}.}
\item{\code{ks}}{the mean over covariates of the proper weighted-eCDF KS
statistic \eqn{\sup_x |F^w_1(x) - F^w_0(x)|}, computed via
\code{\link[cobalt:col_w_ks]{cobalt::col_w_ks()}}; for binary columns this is the absolute
difference in weighted proportions.}
\item{\code{prog}}{the same weighted ASMD formula applied to the single
column \code{prog}.}
}

Faithful to the published method (and its reference implementation), the
\code{smd} and \code{prog} criteria standardize \strong{every} column, including binary
ones, by the plain unweighted sample SD (\code{sd()}, the \eqn{n-1} formula) of
the \code{s.d.denom} group -- i.e., \code{bin.vars = FALSE} is passed to
\code{\link[cobalt:col_w_smd]{cobalt::col_w_smd()}} for all columns. cobalt's own display convention
(binary columns standardized by \eqn{\sqrt{p(1-p)}}) is used only in the
display-oriented \code{balance} component of a \code{psave} object and in
\code{\link[=bal.tab.psave]{bal.tab.psave()}}. For the KS criterion the two conventions coincide.
}
\examples{
set.seed(1)
n <- 200
x1 <- rnorm(n); x2 <- rbinom(n, 1, 0.4)
a <- rbinom(n, 1, plogis(-0.5 + x1 + 0.5 * x2))
ps <- pmin(pmax(fitted(glm(a ~ x1 + x2, family = binomial())), 0.01), 0.99)
g <- 1 + 0.5 * x1 - 0.2 * x2   # a (toy) prognostic score
psave_criteria(ps, a, cbind(x1 = x1, x2 = x2), prog = g, estimand = "ATT")
}
\references{
Kabata D, Stuart EA, Shintani A (2024). Prognostic score-based model
averaging approach for propensity score estimation. \emph{BMC Medical Research
Methodology}, 24, 228. \doi{10.1186/s12874-024-02350-y}

Hansen BB (2008). The prognostic analogue of the propensity score.
\emph{Biometrika}, 95(2), 481-488. \doi{10.1093/biomet/asn004}

Xie Y, Zhu Y, Cotton CA, Wu P (2019). A model averaging approach for
estimating propensity scores by optimizing balance. \emph{Statistical Methods in
Medical Research}, 28(1), 84-101. \doi{10.1177/0962280217715487}
}
\seealso{
\code{\link[=psave]{psave()}}, \code{\link[=simplex_grid]{simplex_grid()}}, \code{\link[cobalt:col_w_smd]{cobalt::col_w_smd()}},
\code{\link[cobalt:col_w_ks]{cobalt::col_w_ks()}}
}
