% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baltab.R
\name{bal.tab.psave}
\alias{bal.tab.psave}
\title{Balance tables for psave objects}
\usage{
\method{bal.tab}{psave}(x, ...)
}
\arguments{
\item{x}{A \code{psave} object.}

\item{...}{Further arguments passed on to \code{\link[cobalt:bal.tab]{cobalt::bal.tab()}} (e.g.,
\code{un = TRUE}, \code{thresholds = c(m = 0.1)}).}
}
\value{
A \code{bal.tab} object; see \code{\link[cobalt:bal.tab]{cobalt::bal.tab()}}.
}
\description{
A method for \code{\link[cobalt:bal.tab]{cobalt::bal.tab()}}: assesses balance on the covariates of a
\code{\link[=psave]{psave()}} fit under the implied inverse-probability weights, with the
model-averaged propensity score and (when available) the model-averaged
prognostic score supplied as \code{distance} measures -- the prognostic-score
balance diagnostic of Stuart, Lee and Leacy (2013).
}
\details{
The call delegates to the default \pkg{cobalt} machinery as
\verb{cobalt::bal.tab(<covariates>, treat = x$treat, weights = x$weights, s.d.denom = x$s.d.denom, distance = data.frame(ps = x$ps, prog = x$prog), ...)}, so all the usual \pkg{cobalt} arguments (\code{un}, \code{stats},
\code{thresholds}, ...) are available, and display conventions are
\pkg{cobalt}'s own (the \emph{selection criterion} inside \code{\link[=psave]{psave()}} uses the
paper's uniform sample-SD standardization instead; see
\code{\link[=psave_criteria]{psave_criteria()}}).
}
\examples{
\dontshow{if (requireNamespace("MatchIt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("lalonde", package = "MatchIt")
fit <- psave(treat ~ age + educ + married + re74, data = lalonde,
             outcome = ~ re78, ps.methods = "glm", prog.methods = "glm")
cobalt::bal.tab(fit, un = TRUE)
\dontshow{\}) # examplesIf}
}
\references{
Stuart EA, Lee BK, Leacy FP (2013). Prognostic score-based balance
measures can be a useful diagnostic for propensity score methods in
comparative effectiveness research. \emph{Journal of Clinical Epidemiology},
66(8), S84-S90. \doi{10.1016/j.jclinepi.2013.01.013}
}
\seealso{
\code{\link[=psave]{psave()}}, \code{\link[cobalt:bal.tab]{cobalt::bal.tab()}}, \code{\link[=plot.psave]{plot.psave()}}
}
