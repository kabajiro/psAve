% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{summary.psave}
\alias{summary.psave}
\alias{print.summary.psave}
\title{Summarize a psave object}
\usage{
\method{summary}{psave}(object, un = TRUE, candidates = TRUE, ...)

\method{print}{summary.psave}(x, digits = 3, ...)
}
\arguments{
\item{object}{A \code{psave} object.}

\item{un}{If \code{TRUE} (default), the balance table includes the unweighted
columns.}

\item{candidates}{If \code{TRUE} (default), the per-candidate \code{diagnostics}
table is included.}

\item{...}{Ignored.}

\item{x}{A \code{summary.psave} object.}

\item{digits}{Number of significant digits to print. Default 3.}
}
\value{
For \code{summary.psave()}, an object of class \code{"summary.psave"}: a
list with elements \code{lambda}, \code{gamma}, \code{gamma.mse}, \code{diagnostics},
\code{balance}, \code{criterion}, \code{criterion.value}, \code{prog.target}, \code{estimand},
\code{average}, \code{nn}, and \code{call}. \code{print.summary.psave()} returns \code{x}
invisibly.
}
\description{
Produces (a) the selected mixing-weight tables \eqn{\lambda} and
\eqn{\gamma}, (b) the \code{diagnostics} table (all four selection criteria for
every candidate propensity score and for the selected average -- the "was
averaging worth it?" comparison), and (c) the full balance table (all
covariates plus the prognostic score; unweighted vs. weighted SMD and KS,
with a \code{*} marker at weighted SMD > 0.1).
}
\seealso{
\code{\link[=psave]{psave()}}, \code{\link[=print.psave]{print.psave()}}, \code{\link[=bal.tab.psave]{bal.tab.psave()}}
}
