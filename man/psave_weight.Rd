% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrappers.R
\name{psave_weight}
\alias{psave_weight}
\title{Weight by a model-averaged propensity score}
\usage{
psave_weight(x, ...)
}
\arguments{
\item{x}{A \code{psave} object.}

\item{...}{Arguments forwarded verbatim to \code{\link[WeightIt:weightit]{WeightIt::weightit()}}.}
}
\value{
A \code{weightit} object; see \code{\link[WeightIt:weightit]{WeightIt::weightit()}}.
}
\description{
Convenience pass-through to \code{\link[WeightIt:weightit]{WeightIt::weightit()}}: constructs balancing
weights from the model-averaged propensity score of a \code{\link[=psave]{psave()}} fit at the
fitted estimand, reusing the stored formula and data. Equivalent to the
canonical explicit call
\preformatted{WeightIt::weightit(<formula>, data = <data>, ps = fit$ps, estimand = fit$estimand, ...)}
All \code{...} arguments are forwarded verbatim; the return value is an ordinary
\code{weightit} object.
}
\examples{
\dontshow{if (requireNamespace("MatchIt", quietly = TRUE) && requireNamespace("WeightIt", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data("lalonde", package = "MatchIt")
fit <- psave(treat ~ age + educ + married + re74, data = lalonde,
             outcome = ~ re78, ps.methods = "glm", prog.methods = "glm")
w <- psave_weight(fit)
cobalt::bal.tab(w, distance = data.frame(prog = fit$prog))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=psave]{psave()}}, \code{\link[=psave_match]{psave_match()}}, \code{\link[WeightIt:weightit]{WeightIt::weightit()}},
\code{\link[WeightIt:get_w_from_ps]{WeightIt::get_w_from_ps()}}
}
